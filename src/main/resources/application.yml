fhir-bridge:
  fhir:
    narrative-generation: false
    url-mapping: /fhir/*
    validation:
      terminology:
        enabled: true
        server-base-url: https://r4.ontoserver.csiro.au/fhir/
logging:
  level:
    ca.uhn.fhir: info
    org.ehrbase.fhirbridge: info
    org.hl7.fhir: info
server:
  servlet:
    context-path: /fhir-bridge
  port: 8080